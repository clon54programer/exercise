#pragma once
/*para usar la programacion orintada a objetos se debe primereo declarar una cabezara
despues debemos crear un archivo con la extension.cpp con el mismo nombre del heder*/
// el header creamos los metodos y variables pero no las definimos.
// los metodos se deben definir en el archivo con la extension .cpp.
class carro
{
public:
    double suma(int x, int y);
};